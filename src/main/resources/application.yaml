server:
  port: 3000
spring:
  application:
    name: ToDoApplication
  datasource:
    password: password
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true